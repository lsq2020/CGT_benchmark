services:
  - type: web
    name: cgt-bench
    env: python
    plan: starter
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
      - key: DATABASE_URL
        fromDatabase:
          name: cgt-bench-db
          property: connectionString

databases:
  - name: cgt-bench-db
    databaseName: cgt_bench
    user: cgt_bench
